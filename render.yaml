services:
  - type: web
    name: churn-api
    env: docker
    plan: free
    region: oregon

    dockerfilePath: ./Dockerfile

    autoDeploy: true

    envVars:
      - key: PORT
        value: 8000